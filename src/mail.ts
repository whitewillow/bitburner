import { NS } from '@ns';
import { PrintRows, PrintTable } from 'lib/lib.print';

export async function main(ns: NS): Promise<void> {
  ns.printRaw('MAILS! -------------------');

  function getMails() {
  
  }

  getMails();
}
